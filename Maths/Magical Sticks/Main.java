public class Main {
    private static void solve(FastScanner sc) {
        int t = sc.nextInt();
        while (t-- > 0) {
            int N = sc.nextInt();
            System.out.println((N + 1) >> 1);
        }
    }
}