public class Main {
    private static void solve(FastScanner sc) {
        int t = sc.nextInt();
        while (t-- > 0) {

            // sum of any two sides > other side

            // int a = sc.nextInt();
            // int b = sc.nextInt();
            // int c = sc.nextInt();
            // int d = sc.nextInt();

            // int x = b;
            // int y = c;
            // int z = c;

            sc.nextInt();
            int x = sc.nextInt();
            int y, z = y = sc.nextInt();
            sc.nextInt();

            System.out.println(x + " " + y + " " + z);
        }
    }
}